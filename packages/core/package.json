{
  "name": "@figma-flow-annotator/core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "node --test tests/annotations/*.test.mjs tests/connectors/*.test.mjs tests/shared/*.test.mjs tests/validation/*.test.mjs"
  }
}
