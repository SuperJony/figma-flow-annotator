# Refresh connectors explicitly

The first demo recalculates **Flow Connector** routes on creation and through an explicit refresh command, not through continuous background listening. Explicit refresh keeps the Figma Design plugin simple and predictable while still allowing moved endpoints, context frames, and annotation cards to produce updated boundary connection points and obstacle-avoiding orthogonal routes.

