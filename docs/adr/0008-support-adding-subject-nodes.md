# Support adding subject nodes to existing annotations

The first demo supports adding selected **Subject Nodes** to an existing **Annotation**. Multi-node annotations are common enough that forcing designers to recreate an annotation after a missed selection would make validation noisy; each added subject node should receive a same-number **Annotation Badge** by default while preserving the existing annotation number and card.

