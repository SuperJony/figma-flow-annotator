# Keep annotation numbers stable

**Annotation Numbers** are scoped to a **Context Frame** and are not automatically compacted when an **Annotation** is deleted. Stable numbers protect references in badges, review discussions, screenshots, and agent output; any later renumbering should be an explicit command rather than a deletion side effect.

