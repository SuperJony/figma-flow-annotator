# Own cross-frame connectors by start context

A cross-frame **Flow Connector** is owned by the start endpoint's **Context Frame** while still storing both endpoint context frame IDs. This matches the flow-reading sentence "from this scene, perform an action to reach that scene" and keeps extraction grouped by the scene where the transition begins.

