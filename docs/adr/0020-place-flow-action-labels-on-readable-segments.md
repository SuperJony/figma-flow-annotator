# Place flow action labels on readable segments

**Flow Action** labels should be placed close to their **Flow Connector** while preserving readability and avoiding overlap with **Connector Obstacles** and other labels. For a single connector, prefer the longest visible segment near the route center; for connectors that share a **Connector Trunk**, place distinct labels on their branch segments rather than on the shared trunk.

