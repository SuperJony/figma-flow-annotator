# Use selection-first workflow for the first demo

The first demo creates **Annotations** and **Flow Connectors** from the user's current Figma selection instead of building a full node picker. This keeps the demo focused on proving node bindings, standardized visual creation, and later agent extraction while leaving richer editing workflows for a later iteration.

