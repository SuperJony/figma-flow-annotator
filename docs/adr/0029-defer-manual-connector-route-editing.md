# Defer manual connector route editing

The first demo does not support manual editing of **Connector Routes**. Routes are generated from endpoints and obstacles, then refreshed explicitly; adding manual route points would create a second source of truth and complicate validation, refresh, and routing failure behavior.

