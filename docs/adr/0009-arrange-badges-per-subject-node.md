# Arrange badges per subject node

When a **Subject Node** has multiple **Annotation Badges**, the first demo arranges them horizontally near the subject node and orders them by **Annotation Number**. This simple rule avoids overlapping badges without introducing drag-based layout controls or a full collision-avoidance system.

