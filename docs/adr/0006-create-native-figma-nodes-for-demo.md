# Create native Figma nodes for the first demo

The first demo creates **Annotation Cards**, **Annotation Badges**, and **Flow Connectors** as native Figma node trees instead of requiring a pre-installed component library. This keeps validation focused on standardized creation and shared metadata bindings; visual components can be introduced later once the annotation model and workflow are proven.

