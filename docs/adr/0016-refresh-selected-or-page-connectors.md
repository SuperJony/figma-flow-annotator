# Refresh selected or page connectors

The refresh command updates selected **Flow Connectors** when any are selected; otherwise it updates every **Flow Connector** on the current page. This keeps local debugging precise while making the common case safe after designers move multiple endpoints, context frames, or annotation cards.

