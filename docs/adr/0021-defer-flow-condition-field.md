# Defer flow condition field

The first demo does not add a separate **Flow Condition** field. Designers can express branch meaning in the optional **Flow Action** text, keeping connector creation lightweight while preserving a future path to split action and condition if extraction needs become more precise.

