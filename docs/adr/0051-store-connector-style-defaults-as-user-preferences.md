# Store Connector Style defaults as user preferences

The panel should remember the current **Connector Style** as a user preference between plugin sessions. This gives designers persistent authoring defaults without storing style on each **Flow Connector** or writing project-wide shared data that would affect collaborators.
