# Arrange cards explicitly by number

**Annotation Cards** are created in the **Design Notes Area** below their **Context Frame**. The first demo should preserve designer placement during normal creation and only sort cards by **Annotation Number** when an explicit arrange command is used, avoiding unexpected movement of existing card layouts.

