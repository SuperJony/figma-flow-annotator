# Layer connectors below annotation nodes

Visual layers should stack from bottom to top as **Context Frames**, page-level **Flow Connectors**, **Annotation Cards**, then **Annotation Badges**. This keeps connectors visible across frames while preventing them from covering annotation details or local badge anchors.

